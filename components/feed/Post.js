export default function Post({ post }) {
  const { id, name, username, userImg, img, text, timestamp } = post;
  return (
    <div className=''>
      <div className=''>
        <div className=''></div>
      </div>
    </div>
  );
}
